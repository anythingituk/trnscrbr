<Window x:Class="Trnscrbr.Views.FloatingButtonWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Width="104"
        Height="72"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        Topmost="True"
        ShowInTaskbar="False"
        ResizeMode="NoResize">
    <Grid x:Name="Root"
          MouseLeftButtonDown="Root_OnMouseLeftButtonDown"
          MouseLeftButtonUp="Root_OnMouseLeftButtonUp"
          MouseMove="Root_OnMouseMove"
          MouseRightButtonUp="Root_OnMouseRightButtonUp">
        <Border x:Name="Shell"
                Width="68"
                Height="68"
                CornerRadius="34"
                HorizontalAlignment="Center"
                VerticalAlignment="Center"
                BorderThickness="1"
                BorderBrush="#88FFFFFF"
                Background="#66202834">
            <Border.Effect>
                <DropShadowEffect x:Name="Glow" Color="#36D5D3" BlurRadius="28" ShadowDepth="0" Opacity="0.95" />
            </Border.Effect>
            <Grid>
                <StackPanel x:Name="Waveform"
                            Orientation="Horizontal"
                            HorizontalAlignment="Center"
                            VerticalAlignment="Center">
                    <Rectangle x:Name="Bar1" Width="4" Height="14" RadiusX="2" RadiusY="2" Fill="#EFFFFFFF" Margin="2" />
                    <Rectangle x:Name="Bar2" Width="4" Height="26" RadiusX="2" RadiusY="2" Fill="#EFFFFFFF" Margin="2" />
                    <Rectangle x:Name="Bar3" Width="4" Height="18" RadiusX="2" RadiusY="2" Fill="#EFFFFFFF" Margin="2" />
                    <Rectangle x:Name="Bar4" Width="4" Height="30" RadiusX="2" RadiusY="2" Fill="#EFFFFFFF" Margin="2" />
                    <Rectangle x:Name="Bar5" Width="4" Height="16" RadiusX="2" RadiusY="2" Fill="#EFFFFFFF" Margin="2" />
                </StackPanel>
                <TextBlock x:Name="TimerText"
                           HorizontalAlignment="Center"
                           VerticalAlignment="Bottom"
                           Margin="0,0,0,8"
                           FontSize="10"
                           FontWeight="SemiBold"
                           Foreground="White"
                           Visibility="Collapsed" />
            </Grid>
        </Border>
        <Border x:Name="MessageBubble"
                MaxWidth="260"
                Padding="10,6"
                CornerRadius="8"
                Background="#EE101820"
                HorizontalAlignment="Center"
                VerticalAlignment="Top"
                Visibility="Collapsed">
            <TextBlock Text="{Binding StatusMessage}" Foreground="White" FontSize="12" TextWrapping="Wrap" />
        </Border>
    </Grid>
</Window>
